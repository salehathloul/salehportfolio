# أوامر Claude Code — مرحلة بمرحلة
# ============================================
# انسخ كل أمر في وقته — لا تعطيه كل شيء دفعة واحدة
# ============================================


# ═══════════════════════════════════════════
# الخطوة صفر — التهيئة الأولى (مرة واحدة فقط)
# ═══════════════════════════════════════════

# أولاً: أنشئ مجلد المشروع وضع فيه ملف CLAUDE.md
# ثم افتح Claude Code داخل المجلد:
#   cd ~/saleh-portfolio
#   claude

# أول أمر تعطيه:

"""
اقرأ ملف CLAUDE.md في هذا المجلد. هذا هو المرجع الكامل للمشروع.
لا تبدأ أي عمل الآن — فقط أكّد أنك فهمت المشروع وأخبرني بأي ملاحظات أو أسئلة قبل أن نبدأ.
"""


# ═══════════════════════════════════════════
# المرحلة ١ — التأسيس والبنية
# ═══════════════════════════════════════════

# ─── الأمر ١.١: إنشاء المشروع ───

"""
ابدأ المرحلة الأولى: التأسيس.

١. أنشئ مشروع Next.js 14+ مع App Router و TypeScript و Tailwind CSS
٢. ثبّت المكتبات الأساسية:
   - prisma و @prisma/client
   - next-auth
   - next-intl
   - framer-motion
   - @tanstack/react-query
   - react-hook-form و zod
   - sharp
٣. أنشئ هيكل المجلدات كما في CLAUDE.md
٤. أنشئ ملف prisma/schema.prisma بمخطط قاعدة البيانات الكامل من CLAUDE.md
٥. أعد ملف tailwind.config.ts مع CSS Variables للوضع الداكن والفاتح
٦. أعد ملف globals.css مع متغيرات الألوان والخطوط

لا تبنِ أي صفحات بعد — فقط البنية التأسيسية.
"""


# ─── الأمر ١.٢: المصادقة وقاعدة البيانات ───

"""
أكمل التأسيس:

١. أعد NextAuth مع Credentials provider (مستخدم واحد admin)
٢. أنشئ ملف lib/db.ts مع Prisma client singleton
٣. أنشئ ملف lib/auth.ts مع إعدادات NextAuth
٤. أنشئ middleware.ts لحماية مسارات /admin
٥. أنشئ صفحة تسجيل الدخول app/admin/login/page.tsx — تصميم بسيط ونظيف
٦. أنشئ layout لوحة التحكم app/admin/layout.tsx مع sidebar navigation يشمل:
   الرئيسية، الإعدادات، المعرض، المدونة، الاقتناء، الطلبات، الرسائل

استخدم .env.local للمتغيرات الحساسة. أنشئ ملف .env.example بالمفاتيح المطلوبة.
"""


# ─── الأمر ١.٣: نظام رفع الملفات ───

"""
أنشئ نظام رفع الملفات والصور:

١. أنشئ lib/cloudinary.ts مع helpers للرفع والحذف وتوليد الأحجام
٢. أنشئ API route: app/api/upload/route.ts
   - يقبل صور ويرفعها على Cloudinary
   - يولّد أحجام متعددة تلقائياً (thumbnail, medium, large, original)
   - يرجع الروابط والأبعاد
٣. أنشئ مكوّن components/admin/ImageUploader.tsx
   - يدعم السحب والإفلات (Drag & Drop)
   - يعرض معاينة قبل الرفع
   - شريط تقدم أثناء الرفع
٤. أنشئ API route لرفع الخطوط: app/api/upload/font/route.ts
   - يقبل .woff2, .otf, .ttf
"""


# ═══════════════════════════════════════════
# المرحلة ٢ — لوحة التحكم
# ═══════════════════════════════════════════

# ─── الأمر ٢.١: الإعدادات العامة ───

"""
ابدأ المرحلة الثانية: لوحة التحكم.

أنشئ صفحة الإعدادات العامة app/admin/settings/page.tsx:

١. قسم الشعار:
   - رفع شعار فاتح وشعار غامق
   - معاينة فورية
٢. قسم الخطوط:
   - رفع خطوط مخصصة
   - تعيين خط العناوين وخط النص
   - معاينة الخط بعد الرفع
٣. قسم وسائل التواصل:
   - حقول: Instagram, X, Behance, LinkedIn, Email
٤. قسم الصفحة الرئيسية:
   - رفع صورة الغلاف
   - كتابة الاقتباس/العبارة (AR/EN)
٥. قسم SEO:
   - عنوان الموقع (AR/EN)
   - الوصف (AR/EN)
   - صورة المشاركة

كل حقل نصي يكون مزدوجاً (AR/EN).
استخدم react-hook-form مع zod للتحقق.
احفظ في جدول SiteSettings.
"""


# ─── الأمر ٢.٢: إدارة المعرض ───

"""
أنشئ إدارة معرض الأعمال في لوحة التحكم:

١. صفحة القائمة app/admin/portfolio/page.tsx:
   - عرض جميع الأعمال كبطاقات مصغرة
   - ترتيب بالسحب والإفلات (استخدم @dnd-kit/core)
   - فلترة حسب التصنيف
   - أزرار: إضافة عمل جديد، تعديل، حذف، إظهار/إخفاء

٢. نموذج إضافة/تعديل عمل (Modal أو صفحة):
   - رفع الصورة (ImageUploader)
   - الحقول: رقم العمل (FA-XXX)، الاسم AR/EN، موقع التصوير AR/EN
   - التصنيف (اختيار من القائمة)، تاريخ التصوير، وصف AR/EN
   - خيارات: منشور، مميز (للصفحة الرئيسية)

٣. إدارة التصنيفات:
   - إضافة/تعديل/حذف تصنيفات (AR/EN)
   - ترتيب التصنيفات

٤. إعدادات العرض:
   - اختيار الأنماط المتاحة للزائر
   - تعيين النمط الافتراضي

أنشئ API routes كاملة في app/api/portfolio/
"""


# ─── الأمر ٢.٣: محرر المدونة ───

"""
أنشئ محرر المدونة — هذا أهم مكوّن تقني في المشروع.

١. ثبّت TipTap مع الإضافات:
   @tiptap/react, @tiptap/starter-kit, @tiptap/extension-image,
   @tiptap/extension-youtube, @tiptap/extension-placeholder,
   @tiptap/extension-text-align, @tiptap/extension-heading,
   @tiptap/extension-blockquote, @tiptap/extension-link,
   @tiptap/extension-color, @tiptap/extension-text-style,
   @tiptap/extension-font-family

٢. أنشئ مكوّن components/admin/BlogEditor.tsx:
   - شريط أدوات (Toolbar) واضح يشمل:
     * تغيير الخط والحجم واللون
     * عناوين H1-H4
     * غامق، مائل، تسطير
     * اقتباس
     * روابط
     * محاذاة النص
   - إدراج صورة فردية من ImageUploader
   - إدراج مجموعة صور (Gallery) — أنشئ TipTap extension مخصص:
     * يعرض الصور كشبكة
     * تحكم بعدد الأعمدة (2, 3, 4)
     * تحكم بالحجم
   - تضمين فيديو YouTube/Vimeo
   - فاصل بصري (Horizontal Rule)

٣. صفحة قائمة التدوينات app/admin/blog/page.tsx:
   - بطاقات: غلاف + عنوان + حالة + تاريخ
   - فلترة حسب الحالة (مسودة/منشور/مخفي)

٤. صفحة المحرر app/admin/blog/editor/[id]/page.tsx:
   - حقول: عنوان AR/EN، غلاف
   - المحرر الغني (BlogEditor)
   - أزرار: حفظ مسودة، معاينة، نشر
   - المحتوى يُحفظ كـ JSON (TipTap format)

٥. API routes: app/api/blog/
"""


# ─── الأمر ٢.٤: إدارة الاقتناء والطلبات ───

"""
أنشئ نظام الاقتناء وإدارة الطلبات:

١. صفحة إدارة الأعمال المتاحة app/admin/acquire/page.tsx:
   - اختيار عمل من المعرض لجعله متاحاً للاقتناء
   - لكل عمل: إدارة المقاسات (إضافة/حذف مقاسات)
   - لكل مقاس: تسمية (مثل "50×70 سم")، عدد النسخ الإجمالي
   - عرض النسخ المتبقية تلقائياً
   - تفعيل/إيقاف عمل من العرض

٢. صفحة إدارة الطلبات app/admin/orders/page.tsx:
   - جدول بجميع الطلبات
   - فلترة حسب الحالة: جديد، قيد المراجعة، مقبول، مرفوض، مكتمل
   - تفاصيل كل طلب: بيانات المقتني، العمل، المقاس، الرسالة
   - تغيير الحالة
   - حقل لكتابة السعر وإرساله للمقتني بالبريد
   - ملاحظات داخلية

٣. صفحة رسائل التواصل app/admin/messages/page.tsx:
   - عرض الرسائل الواردة
   - فلترة حسب التصنيف والحالة
   - تغيير الحالة: جديد، تم الرد، مغلق

٤. نظام البريد lib/email.ts:
   - إشعار للمالك عند طلب جديد
   - إشعار للمالك عند رسالة تواصل
   - إرسال السعر للمقتني
   - تأكيد استلام الطلب للمقتني

٥. API routes كاملة: app/api/orders/ و app/api/contact/
"""


# ═══════════════════════════════════════════
# المرحلة ٣ — الواجهة الأمامية
# ═══════════════════════════════════════════

# ─── الأمر ٣.١: التصميم الأساسي والهيكل ───

"""
ابدأ المرحلة الثالثة: الواجهة الأمامية.

١. نظام التصميم:
   - CSS Variables كاملة للوضع الداكن والفاتح
   - ألوان: خلفية، نص، accent، حدود — لكل وضع
   - الخطوط تُحمّل ديناميكياً من إعدادات الموقع
   - مكوّن ThemeToggle للتبديل بين الأوضاع

٢. نظام ثنائية اللغة:
   - إعداد next-intl مع مسارات [locale]
   - middleware للتوجيه التلقائي
   - ملفات ترجمة ar.json و en.json للنصوص الثابتة
   - مكوّن LanguageSwitcher

٣. Layout الرئيسي app/[locale]/layout.tsx:
   - Header: شعار (يتغير حسب الوضع) + تنقل + تبديل اللغة + تبديل الوضع
   - Footer: روابط التواصل + حقوق النشر
   - التنقل يتجاوب مع الموبايل (قائمة hamburger)
   - الشعار والتنقل يُحمّلان من SiteSettings

٤. أنشئ مكونات UI الأساسية في components/ui/:
   - Button, Input, TextArea, Select, Modal, Toast, Spinner, Badge

التصميم فنّي وبسيط — مساحات واسعة — الصور هي البطل.
استخدم Framer Motion لحركات انتقالية هادئة.
"""


# ─── الأمر ٣.٢: الصفحة الرئيسية والمعرض ───

"""
أنشئ الصفحة الرئيسية ومعرض الأعمال:

١. الصفحة الرئيسية app/[locale]/page.tsx:
   - Hero section: صورة غلاف كاملة العرض مع اقتباس فوقها
   - تأثير parallax خفيف أو fade-in عند التحميل
   - قسم آخر الأعمال: يعرض الأعمال المميزة (isFeatured) كشبكة أنيقة
   - Scroll-triggered animations للأعمال

٢. معرض الأعمال app/[locale]/portfolio/page.tsx:
   - ثلاثة أنماط Grid مع أزرار تبديل:
     * Grid منتظم: CSS Grid بأعمدة متساوية، الصور مقصوصة بنسبة ثابتة
     * Masonry: مكتبة masonry أو CSS columns، الصور بنسبها الأصلية
     * عشوائي: أحجام متنوعة بتخطيط مدروس عشوائياً
   - فلترة حسب التصنيف مع animation عند التبديل
   - زر إظهار/إخفاء معلومات الصور
   - كل صورة تعرض: الاسم، الرقم (FA-XXX)، موقع التصوير
   - Lightbox عند النقر — تنقل بالأسهم، إغلاق بـ ESC، swipe على الموبايل
   - Lazy loading + blur placeholder
   - Framer Motion لحركات الظهور

اجلب البيانات من API بـ Server Components حيث أمكن.
"""


# ─── الأمر ٣.٣: المدونة والاقتناء والباقي ───

"""
أنشئ باقي صفحات الواجهة الأمامية:

١. المدونة app/[locale]/blog/page.tsx:
   - قائمة التدوينات: بطاقات بغلاف وعنوان وتاريخ
   - حركة ظهور متتابعة (staggered animation)
   - صفحة التدوينة app/[locale]/blog/[slug]/page.tsx:
     * عرض محتوى TipTap بتنسيق جميل
     * أنشئ مكوّن TipTapRenderer يحوّل JSON إلى HTML منسّق
     * دعم مجموعات الصور (Gallery) بعرض شبكي
     * دعم الفيديو المضمّن
     * تايبوغرافي مدروسة للقراءة

٢. صفحة الاقتناء app/[locale]/acquire/page.tsx:
   - معرض الأعمال المتاحة
   - كل عمل يعرض: الصورة + المقاسات + النسخ المتبقية
   - عند اختيار عمل: modal أو صفحة بنموذج الطلب
   - نموذج الطلب: اختيار الحجم، الاسم، الإيميل، الجوال، سبب الاهتمام
   - تحقق بـ zod + رسائل خطأ واضحة
   - رسالة تأكيد بعد الإرسال
   - لا يظهر أي سعر

٣. صفحة About app/[locale]/about/page.tsx:
   - صورة شخصية + نبذة تعريفية
   - تصميم editorial نظيف

٤. صفحة التواصل app/[locale]/contact/page.tsx:
   - نموذج: الاسم، الإيميل، الجوال، تصنيف السبب، الرسالة
   - تأكيد إرسال مع animation

جميع الصفحات تجلب البيانات من API/Database.
جميع النماذج ترسل إشعارات بريدية.
"""


# ═══════════════════════════════════════════
# المرحلة ٤ — التحسينات
# ═══════════════════════════════════════════

"""
المرحلة الرابعة: التحسينات والتجهيز للإطلاق.

١. مراجعة التجاوب:
   - اختبر كل صفحة على أحجام: 375px, 768px, 1024px, 1440px, 1920px
   - أصلح أي مشاكل في التنقل والمعرض والنماذج على الموبايل

٢. الأداء:
   - تأكد من استخدام next/image في كل مكان
   - Lazy loading لكل الصور
   - Static generation للصفحات الثابتة
   - حجم Bundle معقول

٣. SEO:
   - Metadata API في كل صفحة
   - Open Graph tags
   - sitemap.xml و robots.txt
   - Structured data (JSON-LD) للصور والمدونة

٤. إتمام ثنائية اللغة:
   - راجع كل النصوص الثابتة في ar.json و en.json
   - تأكد من تبديل RTL/LTR في كل الصفحات
   - تأكد أن كل محتوى ديناميكي يظهر باللغة الصحيحة

٥. الحركات والانتقالات:
   - راجع كل الـ animations وتأكد من سلاستها
   - أضف page transitions بين الصفحات
   - تأكد من عدم وجود jank أو flicker
"""


# ═══════════════════════════════════════════
# المرحلة ٥ — الإطلاق
# ═══════════════════════════════════════════

"""
المرحلة الخامسة: التجهيز للنشر.

١. أعد ملفات البيئة:
   - .env.production مع كل المتغيرات المطلوبة
   - وثّق كل متغير وكيفية الحصول عليه

٢. إعداد قاعدة البيانات السحابية:
   - أنشئ migration script
   - seed script لإنشاء المستخدم الأول

٣. إعداد Vercel:
   - vercel.json إن لزم
   - إعدادات build
   - Environment variables

٤. اختبار شامل:
   - اختبر كل وظيفة: رفع صور، إنشاء تدوينة، إرسال طلب، تغيير إعدادات
   - اختبر على Chrome, Safari, Firefox
   - اختبر على iOS و Android

٥. أنشئ README.md شاملاً:
   - وصف المشروع
   - كيفية التثبيت والتشغيل محلياً
   - كيفية النشر
   - هيكل المشروع
   - المتغيرات المطلوبة
"""
